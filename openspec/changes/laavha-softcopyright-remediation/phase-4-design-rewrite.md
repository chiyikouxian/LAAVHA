# 阶段四阶段记录：设计说明书重写

日期：2026-08-17
状态：已完成

## 完成内容

- 按模板保留并核对软件介绍、开发信息、开发与运行环境、软件架构、系统流程、详细设计、UI软件设计、运行设计和系统测试章节。
- 将训练、推理、TOPSIS、双重滞后、ALERA、基线算法、消息结构、pybind11绑定、批处理和绘图模块与源文件对应。
- 根据`laavha-handover.cc`补充`flowmonMode`的关闭、记录和指标馈入三种运行口径。
- 区分传播代理得到的SINR/RSRP与FlowMonitor提供的仿真业务流统计，避免将仿真字段概括为真实无线物理层测量。
- 删除固定实验统计结论，改为引用验证报告和实际CSV结果，避免有限次数运行被表述为通用性能结论。
- 保留命令行操作方式、结果图展示和未实现功能边界说明。

## 校验结果

- 设计说明书DOCX重新生成并由LibreOffice转换成功。
- 当前设计说明书PDF为18页，页面尺寸为A4。
- PDF正文可通过`pdftotext`提取。
- 目录包含“软件架构”“软件的详细设计”“UI软件设计与运行操作”“系统测试与性能分析”等章节。
- 设计书引用的架构图、流程图及结果图路径存在。
- Python生成脚本通过`py_compile`，修改文件通过`git diff --check`。

## 待后续处理

阶段五仍需采集并核对真实运行终端、在线决策、运行完成和CSV/图表输出证据；阶段二的目录最终页码在界面材料定稿后再次复核。
