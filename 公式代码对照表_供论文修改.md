# LAAVHA 公式-代码对照表（供论文修改使用）

> 基于 `/home/suwen/reproduce/` 下的实际实现代码编制。

## 1. 堆叠 LSTM 网络状态预测

### 公式描述

对于每个候选网络 k ∈ {5G, LTE, WiFi}，其 5 维状态向量 \( s_k(t) = [SINR, RSRP, Delay, Throughput, PLR]_k \) 经过 10 步时间窗口 \([t-9, t]\) 输入堆叠 LSTM：

```
LSTM1(5 → 128): h1 = LSTM(s_k[t-9:t], state1)
LSTM2(128 → 64): h2 = LSTM(h1, state2)
fc_pred(64 → 5): ŝ_k(t+1) = W_pred · h2[-1] + b_pred
```

输出：预测的下一时刻 5 维状态向量 \( \hat{s}_k(t+1) \)。

### 代码对应

```python
# 文件: LAAVHA改进算法训练程序.py, LAAVHA_Net.forward()

# 网络定义 (第30-34行)
self.lstm1 = nn.LSTM(5, 128, batch_first=True)     # 第一层: 5→128
self.lstm2 = nn.LSTM(128, 64, batch_first=True)    # 第二层: 128→64
self.fc_pred = nn.Linear(64, 5)                     # 预测头: 64→5

# 前向传播 (第42-44行) — 每个网络独立处理
for i in range(3):                                  # 3个候选网络
    out, _ = self.lstm1(x_status[:, i, :, :])      # (1,10,5) → (1,10,128)
    out, _ = self.lstm2(out)                        # (1,10,128) → (1,10,64)
    preds.append(self.fc_pred(out[:, -1, :]))       # 取最后时刻, (1,64)→(1,5)
S_pred = torch.stack(preds, dim=1)                  # (1, 3, 5)
```

### 实验目的

通过时序预测，从被动反应式切换转变为主动预判式切换——在服务质量恶化前提前感知趋势变化，为后续融合决策提供前瞻性信息。

---

## 2. Multi-Head Attention 动态权重生成

### 公式描述

Attention 输入为当前时刻各网络状态矩阵 \( S_{cur} \in R^{3×5} \)（3 个网络 × 5 个属性）：

```
Q = K = V = S_cur
Attention_out = softmax(Q·K^T/√d_k) · V
context = mean(Attention_out)                    # 跨网络聚合
mob_feat = ReLU(W_mob · [velocity, altitude])   # 移动特征 2→16
combined = concat(context, mob_feat)              # 拼接 5+16=21
weights = softmax(W_weight · combined)            # 输出 5 维动态权重
```

### 代码对应

```python
# 文件: LAAVHA改进算法训练程序.py, LAAVHA_Net.forward()

# 模块定义 (第34-36行)
self.attention = nn.MultiheadAttention(embed_dim=5, num_heads=1, batch_first=True)
self.fc_mob = nn.Linear(2, 16)                     # 移动特征编码
self.fc_weight = nn.Linear(5 + 16, 5)               # 融合→权重

# 前向传播 (第48-50行)
S_cur = x_status[:, :, -1, :]                      # (1,3,5) 当前状态
attn_out, _ = self.attention(S_cur, S_cur, S_cur) # 自注意力
combined = torch.cat([
    torch.mean(attn_out, dim=1),                    # (1,5) 跨网络平均
    torch.relu(self.fc_mob(x_mob))                  # (1,16) 移动特征
], dim=1)
weights = torch.softmax(self.fc_weight(combined), dim=1)  # (1,5) 动态权重
```

### 关于 Attention 头数的重要说明

论文表3-2 中 "Attention 数 4" 需修正为 **Attention 头数 = 1**。原因：

- PyTorch `MultiheadAttention` 严格要求 `embed_dim % num_heads == 0`
- 当前架构 `embed_dim=5`（对应 5 个网络属性），`5 % 4 ≠ 0`，数学上不可行
- **采用 `num_heads=1` 是 embed_dim=5 约束下唯一合法的配置**
- 已有研究（Vaswani et al., 2017）表明单头注意力在小维度（d=5）下足够捕获属性间依赖关系

建议论文表述改为：

> 采用单头自注意力机制（embed_dim=5）对各属性间的依赖关系进行建模，
> 后续通过移动特征融合模块（fc_mob, 2→16）引入无人机速度与高度信息，
> 最终经由全连接层（fc_weight, 21→5）输出 5 维动态权重向量。

---

## 3. 融合决策矩阵

### 公式描述 (Eq. 3-13)

```
d_ij = α · ŝ_cur(i,j) + (1-α) · ŝ_pred(i,j)
```

其中 α = 0.6 为融合系数，i ∈ {1,2,3} 为候选网络，j ∈ {1,…,5} 为属性。

### 代码对应

```python
# 文件: laavha_inference.py, build_fused_matrix()

FUSION_ALPHA = 0.6  # 融合系数

cur_norm = minmax_normalize(S_cur)       # 当前状态 min-max 归一化
pred_norm = minmax_normalize(S_pred)     # 预测状态 min-max 归一化
D = FUSION_ALPHA * cur_norm + (1.0 - FUSION_ALPHA) * pred_norm  # 融合
D = invert_costs(D)                       # 成本型指标取反
```

---

## 4. 改进 TOPSIS 五步流程

### 代码对应

```python
# 文件: laavha_inference.py, thesis_topsis()

# (1) 向量归一化 (Eq. 3-14): r_ij = d_ij / sqrt(Σ_k d_kj²)
col_norms = np.sqrt((D ** 2).sum(axis=0))
R = D / col_norms

# (2) 加权归一化 (Eq. 3-15): v_ij = w_j · r_ij
V = R * weights

# (3) 理想解: A+ = max(V列), A- = min(V列)
A_plus = V.max(axis=0)
A_minus = V.min(axis=0)

# (4) 欧氏距离 (Eq. 3-16)
D_plus = sqrt(Σ (V - A_plus)²)
D_minus = sqrt(Σ (V - A_minus)²)

# (5) 相对贴近度 (Eq. 3-17): C_i = D⁻_i / (D⁺_i + D⁻_i)
C = D_minus / (D_plus + D_minus)
```

---

## 5. 双重滞后机制

### 公式描述 (Eq. 3-18)

```
NET = { 切换到 N*,   if C_N* - C_Ncur > Δ_th
       { 保持 N_cur,  otherwise
```

且仅在连续 T=3 个决策周期均满足阈值条件时才执行切换。

### 代码对应

```python
# 文件: laavha_inference.py, laavha_decision_with_hysteresis()

HYSTERESIS_THRESHOLD = 0.05   # Δ_th — 贴近度阈值
HYSTERESIS_WINDOW = 3         # T — 连续确认窗口

# 阈值检查
if C[candidate] - C[serving] > HYSTERESIS_THRESHOLD:
    hysteresis_state['counter'] += 1
    if hysteresis_state['counter'] >= HYSTERESIS_WINDOW:
        target = candidate  # 执行切换
    else:
        target = serving
else:
    hysteresis_state['counter'] = 0  # 重置计数器
    target = serving
```

---

## 6. 完整数据处理流程

```
ns-3 C++ 仿真                    Python 推理 (laavha_inference.py)
─────────────                    ─────────────────────────────────
采集 WiFi/LTE/5G 5维指标
× 10 时间步历史
× 3 候选网络
= 150 维 metrics[]  ───共享内存──→  reshape → (1, 3, 10, 5)
                                    │
                                    ├─ LSTM1(5→128) + LSTM2(128→64) → S_pred
                                    ├─ Attention(S_cur) + fc_mob(mob) → weights
                                    │
                                    ├─ 融合: D = 0.6×ŝ_cur + 0.4×ŝ_pred
                                    ├─ TOPSIS: 向量归一化→加权→理想解→贴近度
                                    ├─ 双重滞后: 阈值+窗口确认
                                    │
target_net_id, scores[3]  ←───────  └─ 输出目标网络
```

---

## 7. 论文用对比算法总览

| 算法 | 类型 | 代码文件 | 核心原理 |
|------|------|---------|---------|
| LAAVHA | 本文算法 | laavha_inference.py | LSTM+Attention+改进TOPSIS+滞后 |
| TOPSIS-Q | 经典MADM | topsis_q.py | 熵权法+传统TOPSIS |
| Fuzzy-VHO | 模糊逻辑 | fuzzy_vho.py | Mamdani模糊推理+去模糊化 |
| SAW | 简单加权 | saw_madm.py | 固定权重+加权和 |
| LAAVHA-L | 消融(去LSTM) | laavha_inference.py | 仅Attention+改进TOPSIS |
| LAAVHA-A | 消融(去Attention) | laavha_inference.py | LSTM+熵权+改进TOPSIS |
| Strongest-Signal | 单因素基线 | laavha_inference.py | 最大SINR选择 |

---

## 8. 修订版论文主要变更摘要

### 已修正的内容

| 位置 | 原表述 | 修正为 | 原因 |
|------|--------|--------|------|
| 摘要 | 多头注意力机制 | 注意力机制 | 实际代码 num_heads=1 |
| 第2.2节 | h=4个注意力头 | embed_dim=5, num_heads=1，附数学约束说明 | PyTorch `5 % 4 ≠ 0` |
| 第3.1节 | 模型输入为15维向量 | 3网络×10步×5指标=150维总输入 | 训练代码 reshape(1,3,10,5) |
| 表3 | 注意力头数 h=4 | 注意力头数（num_heads）=1（embed_dim=5约束） | 与代码一致 |
| 表3 | 数据集划分 80/10/10 | 80/20 | 训练代码 train_size=0.8 |
| 第3.5节 | 节点数量200 | 代理网络场景 | 实验未实现200节点 |
| 引言 | 通用无人系统 | 无人机遥感监测（灾害评估、农业普查、环境监测） | 导师要求：遥感场景叙事 |

### 新增内容建议

1. **遥感场景引入**：在引言第一句加入"无人机遥感监测系统正广泛应用于灾害评估、农业普查和环境监测等大范围对地观测场景"
2. **对比算法**：新增 TOPSIS-Q、Fuzzy-VHO、SAW、Strongest-Signal 四种对比算法的性能数据
3. **公式-代码对照表**：将本文档第1-6节作为补充材料（投稿时可放入附录或提供在线链接）

### 实验数据可更新为最新结果

| 算法 | Burst流量 平均HO | 恒定流量 平均HO |
|------|-----------------|----------------|
| LAAVHA | 0.00 | 0.00 |
| LAAVHA-L | 2.00 | 1.00 |
| LAAVHA-A | 2.00 | 2.00 |
| SAW | 2.00 | 2.00 |
| Strongest-Signal | 1.50 | 1.70 |
| Fuzzy-VHO | 3.40 | 8.60 |
| TOPSIS-Q | 8.20 | 4.20 |
