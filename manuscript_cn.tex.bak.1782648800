%=================================================================
% 物联网学报 — 面向无人机遥感异构网络的注意力LSTM增强型风险感知垂直切换算法
% LaTeX 中文版（ctexart）
%=================================================================
\documentclass[12pt,a4paper]{ctexart}
\usepackage{amsmath,amssymb,booktabs,graphicx,geometry,hyperref,enumitem}
\geometry{left=2.5cm,right=2.5cm,top=2.5cm,bottom=2.5cm}
\graphicspath{{./plots_chapter3_v2/}}
\setlength{\parindent}{2em}
\setlength{\parskip}{0pt}

\title{\heiti\zihao{3} 面向无人机遥感异构网络的注意力LSTM增强型\\风险感知垂直切换算法}
\author{\fangsong 苏文\\[2mm] \small 成都理工大学 计算机与网络安全学院，成都 610059}
\date{}

\begin{document}
\maketitle

%========== 摘要 ==========
\noindent{\heiti 摘\quad 要：}{\songti 无人机遥感监测系统在灾害评估、环境监测等大范围对地观测场景中广泛应用。无人机巡航时需持续穿越5G、LTE和WiFi三种异构网络的覆盖边界，构成典型的候选网络垂直切换场景，面临决策滞后、乒乓切换频繁以及固定属性权重难以适应动态飞行阶段的挑战。提出一种基于长短期记忆网络（LSTM）与注意力机制的自适应垂直切换算法（LAAVHA），并在其决策框架之上引入自适应滞后（ADH）与风险敏感TOPSIS（RS-TOPSIS）两项增强机制，形成增强型风险感知垂直切换算法（ALERA）。该方法利用堆叠LSTM预测候选网络短期状态变化，通过注意力机制根据飞行阶段和移动状态动态生成属性权重，融合当前与预测状态构建改进TOPSIS决策矩阵，结合双重滞后机制抑制不必要切换；ADH使切换阈值和确认窗口随信道波动自适应调节，RS-TOPSIS通过置信下界罚分对评分波动网络进行风险惩罚。基于ns-3与ns3-ai的550次决策级实验表明，ALERA平均切换仅0.24次，显著优于TOPSIS-Q（4.04次）、Fuzzy-VHO（8.64次）等8种对比算法；消融实验与合成压力测试分别验证了时序预测、动态权重及增强机制的协同贡献。}\\[3mm]
\noindent{\heiti 关键词：}{\songti 无人机遥感；异构网络；垂直切换；LSTM；注意力机制；自适应滞后；风险敏感TOPSIS}\\[3mm]
\section{引言}
\label{sec:intro}

无人机遥感监测系统在灾害评估、农业普查和环境监测等大范围对地观测场景中广泛应用。无人机在巡航过程中搭载高分辨率光学/多光谱载荷产生大量遥感图像数据，需通过5G、LTE和WiFi等异构无线网络实时回传至地面站[1-3]。然而，无人机的高机动性导致其频繁穿越不同网络的覆盖边界，触发垂直切换[4]。

传统垂直切换研究主要围绕多属性决策（MADM）方法展开，如TOPSIS[7-8]、VIKOR[22]、GRA[22]、COPRAS[22]、SPOTIS[22]、Fuzzy-VHO[4]和SAW等，通过构建信噪比、时延、吞吐量等网络属性的评价矩阵进行候选排序。这些方法存在两个共性问题：一是权重通常由熵权法或层次分析法一次性确定[10-11]，无法随飞行阶段和信道环境动态调整；二是仅依据当前时刻的网络指标做决策，缺乏对未来状态变化的前瞻性判断，当无人机高速飞向WiFi覆盖边缘时，可能在链路实际中断后才触发切换[12-13]。此外，深度强化学习方法虽被引入切换决策[12,14]，但其黑箱性质和训练不稳定性在安全攸关的遥感巡航场景中带来可解释性和可靠性方面的顾虑。

长短期记忆网络（LSTM）通过门控结构有效缓解梯度消失问题[15]，具有捕获长时间序列依赖关系的能力，已被应用于5G异构网络的切换判决和网络状态预测[16-17]。然而，单一LSTM网络存在两方面局限：其一，LSTM将候选网络的5维状态指标视为独立的时间序列分别建模，无法显式捕获指标之间的耦合关系——例如吞吐量与丢包率的负相关性、SINR与时延的关联性等；其二，LSTM输出的预测状态仍需配合固定或人工设定的属性权重才能完成多属性决策，无法根据飞行阶段和移动状态自适应调整各指标的重要性。

综上所述，当前无人机遥感异构网络中的垂直切换面临两个层面相互关联的挑战。在算法层面，传统MADM方法权重固定、缺乏时序预测能力，单一LSTM即使引入预测机制仍无法自适应调节属性权重以适应飞行阶段变化；在场景层面，无人机巡航过程中信道环境动态变化——平稳飞行时信号波动小，跨越覆盖边界或遭遇遮挡时信号剧烈振荡，固定的切换阈值和确认窗口难以在两者之间取得平衡。

针对上述两个层面相互关联的挑战，本文提出分层次解决方案。在算法层面，构建基于长短期记忆神经网络和注意力机制的自适应垂直切换算法（LAAVHA, Long Short-Term Memory-Attention based Adaptive Vertical Handover Algorithm），以注意力机制弥补传统MADM方法在动态权重与时序预测方面的不足。LAAVHA采用“预测-权重-决策”三阶段架构：堆叠LSTM预测候选网络短期状态变化；注意力机制以当前网络状态矩阵为自注意力输入，融合飞行速度与高度等移动特征，输出随飞行阶段自适应变化的动态属性权重；融合当前与预测状态构建改进TOPSIS决策矩阵，结合双重滞后机制完成候选排序。LAAVHA通过动态权重与前瞻预测，克服了传统MADM方法权重固定和被动响应的问题，实现了无人机遥感巡航场景下的稳定候选网络评价。在场景层面，于LAAVHA决策框架之上进一步引入自适应滞后（ADH）与风险敏感TOPSIS（RS-TOPSIS）两项增强机制，形成注意力LSTM增强型风险感知垂直切换算法（ALERA, Attention-LSTM Enhanced Risk-aware Vertical Handoff Algorithm）。ADH将固定的切换阈值与确认窗口替换为上下文感知的可变参数——以服务网络近5个周期的SINR标准差度量信道波动，阈值随波动从0.03线性增长至0.08，确认窗口随飞行速度在$[2,4]$范围内自适应缩短。RS-TOPSIS引入置信下界罚分，以$C_i^{\text{robust}} = C_i - 0.5 \cdot \sigma_i^C$作为排序依据，对评分波动较大的候选网络施加惩罚，使排序偏好倾向历史表现更稳定的网络。两项增强机制均作用于决策层，不改变LAAVHA模型结构。

%========== 1 系统模型 ==========
\section{系统模型与网络状态参数}
\label{sec:system}

考虑无人机遥感巡航场景：无人机在2000~m $\times$ 2000~m的任务区域内沿预定航线飞行，飞行高度为50$\sim$150~m，巡航速度为5$\sim$30~m/s。任务区域内存在5G、LTE和WiFi三种候选接入网络，其覆盖范围与部署密度各不相同——WiFi热点覆盖半径约200~m，部署于地面站和关键拍摄点周边；LTE基站覆盖半径约1$\sim$2~km，沿航线稀疏分布；5G基站提供高速率但覆盖尚不连续。

无人节点移动会引起候选网络信号质量和传输性能持续变化。在遥感巡航场景中，若仅依据当前时刻指标进行切换决策，无人机可能在高速接近WiFi覆盖边缘时未能提前切换，导致遥感图像回传中断。因此，算法将过去$K=10$个决策周期（$\tau=0.1$~s，即1.0~s历史）的网络状态作为输入，通过学习时序变化趋势实现短期状态预测。

每个候选网络$i \in \{\text{5G, LTE, WiFi}\}$在每个决策周期$t$受一个五维状态向量$S_i(t)$表征：
\begin{equation}
S_i(t) = [\text{SINR}_i(t), \text{RSRP}_i(t), D_i(t), T_i(t), \text{PLR}_i(t)] \label{eq:state}
\end{equation}

在遥感飞行场景中，上述五个指标从不同维度刻画了候选网络服务遥感任务的能力：SINR反映信号质量随距离和遮挡的衰减过程，RSRP与SINR共同决定链路预算是否满足解调门限，端到端时延决定了飞控指令的响应速度与飞行安全裕度，吞吐量衡量有效载荷比特率，丢包率反映传输可靠性。SINR、RSRP和吞吐量属于效益型指标，时延和丢包率属于成本型指标。采用min-max归一化方法将所有指标映射至$[0,1]$区间[22]。

实验平台采用ns-3网络仿真器[19]与其AI扩展模块ns3-ai[20]协同构建。ns-3负责部署5G/LTE/WiFi异构网络拓扑、模拟无人机移动模型和采集物理层/传输层指标；ns3-ai通过共享内存机制在仿真侧与Python AI推理侧之间建立双向通信通道——仿真侧每个决策周期将3个候选网络各10步历史$\times$5维指标（共150维）及移动状态（速度、高度）写入共享内存，Python侧加载预训练的LAAVHA模型读取状态数据并返回目标网络编号和候选网络评分，使深度学习推理与网络仿真同步执行。

%========== 2 算法 ==========
\section{LAAVHA自适应垂直切换算法}
\label{sec:laavha}

基于第1章定义的系统模型与网络状态参数，本章提出基于长短期记忆神经网络和注意力机制的自适应垂直切换算法（LAAVHA），旨在解决传统MADM方法在无人机遥感巡航场景中权重固定和缺乏时序预测能力的不足。LAAVHA以5维网络状态向量和滑动窗口机制为输入，依次通过堆叠LSTM预测（2.1节）、注意力动态权重生成（2.2节）和融合预测状态的改进TOPSIS决策（2.3节）三个模块完成候选网络评估与切换判决，最后在2.4节引入自适应滞后（ADH）与风险敏感TOPSIS（RS-TOPSIS）两项增强机制形成ALERA。

\begin{figure}[htb]
\centering
\includegraphics[width=0.8\textwidth]{fig_laavha_framework.png}
\caption{LAAVHA算法框架}
\end{figure}

\subsection{基于堆叠LSTM的网络状态预测}

设时间窗口长度$T=10$（即1.0~s历史，决策周期$\tau=0.1$~s），候选网络$k \in \{1,2,3\}$（分别对应5G、LTE、WiFi）的历史输入张量为$X_k \in \mathbb{R}^{10 \times 5}$——10个连续决策周期的5维归一化状态向量按行堆叠。预测目标是下一时刻的状态向量$\hat{s}_k(t+\tau) \in \mathbb{R}^5$。

\begin{equation}
\begin{aligned}
h_{1,k} &= \text{LSTM}_1(X_k; \theta_1), \quad h_{1,k} \in \mathbb{R}^{10 \times 128} \\
h_{2,k} &= \text{LSTM}_2(h_{1,k}; \theta_2), \quad h_{2,k} \in \mathbb{R}^{10 \times 64} \\
\hat{s}_k(t+\tau) &= W_{\text{pred}} \cdot h_{2,k}[-1] + b_{\text{pred}}, \quad \hat{s}_k \in \mathbb{R}^5
\end{aligned}
\label{eq:lstm}
\end{equation}
式中$\theta_1$、$\theta_2$分别为两层LSTM的可训练参数，$W_{\text{pred}} \in \mathbb{R}^{5 \times 64}$与$b_{\text{pred}} \in \mathbb{R}^5$为预测头的全连接权重与偏置。三个候选网络共享同一组LSTM参数，各自独立输入历史序列$X_k$，预测结果堆叠为$S_{\text{pred}} \in \mathbb{R}^{3 \times 5}$。

上述LSTM预测模块为TOPSIS决策提供了未来状态的预估值$S_{\text{pred}}$，使算法能够前瞻性地判断网络质量变化趋势。然而，TOPSIS决策矩阵的构建还需确定五个网络属性的相对重要性权重——权重的合理分配直接决定了候选排序的准确性。为此，以下引入注意力机制动态生成权重向量$w \in \mathbb{R}^5$。

\subsection{面向飞行阶段的动态权重生成}

为解决固定权重无法匹配遥感飞行中不同阶段需求差异的问题，LAAVHA引入自注意力机制[18]。将当前3个候选网络的5维状态向量按行堆叠为矩阵$S_{\text{cur}} \in \mathbb{R}^{3 \times 5}$，令自注意力的查询、键、值均等于$S_{\text{cur}}$（即$Q=K=V=S_{\text{cur}}$，embed\_dim=5，num\_heads=1），通过缩放点积注意力计算属性间的交互权重。再经跨网络均值池化提取属性级上下文$c \in \mathbb{R}^5$，并联飞行速度$v$与高度$h$经$W_{\text{mob}} \in \mathbb{R}^{2 \times 16}$映射为移动特征$m \in \mathbb{R}^{16}$，拼接后经全连接层投影与softmax归一化输出5维动态权重$w$：
\begin{equation}
\begin{aligned}
Q &= K = V = S_{\text{cur}} \in \mathbb{R}^{3 \times 5} \\
A &= \text{softmax}\!\left(\frac{QK^\top}{\sqrt{5}}\right) V, \quad A \in \mathbb{R}^{3 \times 5} \\
c &= \text{mean}(A, \text{dim}=0) \in \mathbb{R}^5 \\
m &= \text{ReLU}(W_{\text{mob}} \cdot [v, h]^\top), \quad m \in \mathbb{R}^{16} \\
w &= \text{softmax}(W_{\text{weight}} \cdot [c \parallel m]), \quad w \in \mathbb{R}^5, \quad \sum_j w_j = 1
\end{aligned}
\label{eq:attention}
\end{equation}

通过上述两节的时序预测（输出$S_{\text{pred}}$）和动态权重生成（输出$w$），已获得构建TOPSIS决策所需的全部要素。以下将这些输出整合为统一的决策框架。

\subsection{融合预测状态的改进TOPSIS决策}

为避免传统TOPSIS仅依赖当前状态导致的被动决策，LAAVHA将当前状态与LSTM预测状态进行融合，构建决策矩阵：
\begin{equation}
\begin{aligned}
s_{\text{norm}} &= \frac{s - s_{\min}}{s_{\max} - s_{\min}}, \quad \text{for each indicator } j \\
d_{ij} &= \alpha \cdot s_{\text{cur}}(i,j) + (1-\alpha) \cdot s_{\text{pred}}(i,j), \quad \alpha = 0.6
\end{aligned}
\label{eq:fusion}
\end{equation}
式中$\alpha$为融合系数（本文取$\alpha=0.6$），使当前实测状态占主导、预测信息提供前瞻引导。将成本型指标取反后得到融合决策矩阵$D \in \mathbb{R}^{3 \times 5}$，进入标准化TOPSIS流程：
\begin{equation}
\begin{aligned}
r_{ij} &= d_{ij} \big/ \sqrt{\sum_{k=1}^{3} d_{kj}^2} \\
v_{ij} &= w_j \cdot r_{ij} \\
v^+_j &= \max_i v_{ij}, \quad v^-_j = \min_i v_{ij} \\
D^+_i &= \sqrt{\sum_{j=1}^{5} (v_{ij} - v^+_j)^2}, \quad D^-_i = \sqrt{\sum_{j=1}^{5} (v_{ij} - v^-_j)^2}
\end{aligned}
\label{eq:topsis1}
\end{equation}

\begin{equation}
\begin{aligned}
C_i &= \frac{D^-_i}{D^+_i + D^-_i}, \quad C_i \in [0,1] \\
N^* &= \arg\max_i C_i \\
&\text{IF } C(N^*) - C(N_{\text{cur}}) > \Delta_{th} \text{ AND } T \text{ consecutive cycles} \quad \text{THEN switch to } N^* \\
&\text{ELSE keep } N_{\text{cur}} \quad (\Delta_{th} = 0.05, \; T = 3)
\end{aligned}
\label{eq:topsis2}
\end{equation}

式\eqref{eq:topsis1}--\eqref{eq:topsis2}将$3 \times 5$的融合决策矩阵$D$转换为三个候选网络的相对贴近度排序。$C_i$越接近1，表示该网络对遥感任务中图像回传、飞控响应和链路可靠性的综合保障能力越强。为降低网络状态瞬时波动引发的乒乓切换，算法设置双重滞后机制——仅当得分最高的候选网络$N^*$的贴近度超过当前服务网络$N_{\text{cur}}$达$\Delta_{th}=0.05$以上，且该条件在连续$T=3$个决策周期内持续满足时，才正式执行切换。

\subsection{自适应滞后与风险敏感TOPSIS增强机制}

上述LAAVHA框架中的双重滞后机制采用固定参数（$\Delta_{th}=0.05, T=3$），在信道平稳场景下表现良好。但在无人机遥感巡航场景中，信道状态随飞行位置和周围环境持续变化，固定的滞后参数无法感知这种环境变化。因此本节引入自适应滞后（ADH）与风险敏感TOPSIS（RS-TOPSIS）两项决策层增强机制，形成ALERA。

\subsubsection{自适应滞后参数}

将固定阈值$\Delta_{th}$和窗口$T$替换为上下文感知的可变参数。定义近期SINR波动度$\sigma$为当前服务网络近5个决策周期SINR的标准差，飞行速度$v$由仿真侧实时传入。自适应阈值和窗口计算公式为：
\begin{equation}
\begin{aligned}
\Delta_{th} &= 0.03 + 0.05 \cdot (\sigma / 10), \quad \sigma \in [0, 10] \\
T &= \max(2, \lfloor 4 - 2 \cdot v / 30 \rfloor), \quad v \in [0, 30] \text{ m/s}
\end{aligned}
\label{eq:adh}
\end{equation}
式\eqref{eq:adh}(a)在信道稳定时给出基础阈值0.03，低于原始固定值0.05，为快速响应真实退化预留空间；波动修正项使阈值随SINR波动线性增长至最高0.08，在信道振荡时抑制乒乓。式\eqref{eq:adh}(b)在低速飞行时给出窗口4，提供充分的确认周期；窗口随速度增加而缩短至最低2，使高速无人机飞越覆盖边界时能及时切换。参数范围（$\Delta_{th} \in [0.03, 0.08]$, $T \in [2, 4]$）确保了极端条件下的基本防抖能力。窗口随速度缩短的设计依据在于：高速飞行时确认期间位移大——30~m/s时$T=4$（0.4~s）位移达12~m，初始判断可能已失效；缩短至$T=2$（0.2~s）使算法在覆盖边界附近更快锁定切换时刻。

\subsubsection{风险敏感TOPSIS}

原始TOPSIS以当前贴近度$C_i$作为排序依据。增强版引入置信下界思想，对候选网络评分减去其历史波动惩罚项：
\begin{equation}
C_i^{\text{robust}} = C_i - \lambda \cdot \sigma_i^C, \quad \lambda = 0.5
\label{eq:rstopsis}
\end{equation}
其中$\sigma_i^C$为网络$i$近5轮评分的标准差，$\lambda=0.5$为风险厌恶系数。该机制的核心思想是——在多个候选网络贴近度接近时，优先选择评分稳定的网络而非均值略高但波动剧烈的网络。在遥感巡航场景中，若LTE在近5个周期内贴近度持续上升、方差较小，而WiFi贴近度在高低之间大幅跳动，即使WiFi的当前贴近度略高于LTE，$C^{\text{robust}}$排序仍将偏好LTE，保障遥感数据传输的连续性。

两项增强机制的关键优势在于：无需重新训练LSTM-Attention模型，仅通过决策层参数和排序逻辑的调整即可为算法增加环境感知能力。在信道平稳时，自适应参数退化为接近原始固定值，与原版表现一致；在信道剧烈波动时，自适应阈值、缩短窗口和风险罚分协同作用，抑制乒乓切换。

%========== 2.5 算法复杂度与总结 ==========
\subsection{算法复杂度与总结}

表1汇总了LAAVHA及ALERA各算法模块的输入/输出、核心流程与计算复杂度。整体算法的端到端推理复杂度控制在$O(R \cdot K^2 \cdot d)$以内（$R=3$为候选网络数，$K=10$为时间窗口长度，$d=5$为状态维度，$h=128$为LSTM隐藏层大小），远低于单个决策周期0.1~s的时间预算，满足无人机遥感巡航场景下的实时切换决策需求。

\begin{table}[htb]
\centering
\caption{算法总结}
\small
\begin{tabular}{p{3cm}p{2.5cm}p{4cm}p{2cm}}
\toprule
\textbf{模块} & \textbf{输入/输出} & \textbf{核心流程} & \textbf{复杂度} \\
\midrule
堆叠LSTM预测(2.1) & In: $3\times10\times5$张量 Out: $S_{\text{pred}}$ & LSTM1$\rightarrow$LSTM2$\rightarrow$fc & $O(RKdh^2)$ \\
注意力动态权重(2.2) & In: $3\times5$矩阵+移动特征 Out: $w\in\mathbb{R}^5$ & Self-Attn$\rightarrow$Pool$\rightarrow$FC+softmax & $O(R^2 d)$ \\
改进TOPSIS(2.3) & In: $S_{\text{cur}},S_{\text{pred}},w$ Out: $C_i$ & Fuse$\rightarrow$Norm$\rightarrow$Weight$\rightarrow$Closeness & $O(R d)$ \\
ADH自适应滞后(2.4.1) & In: SINR历史,$v$ Out: $\Delta_{th},T$ & Volatility$\rightarrow$Linear$\rightarrow$Window & $O(1)$ \\
RS-TOPSIS(2.4.2) & In: 5轮$C_i$历史 Out: $C_i^{\text{robust}}$ & Std$\rightarrow$Penalty$\rightarrow$LCB ranking & $O(R K)$ \\
\bottomrule
\end{tabular}
\end{table}

%========== 3 实验 ==========
\section{仿真实验与结果分析}
\label{sec:experiments}

\subsection{实验平台与参数设置}

为验证ALERA决策流程的有效性，基于ns-3.45[19]和ns3-ai[20]搭建了网络仿真与Python模型推理协同平台。仿真侧周期性采集候选网络指标并写入共享内存，Python侧加载预训练的LAAVHA模型执行推理。表2列出了候选网络的五项指标来源。模型输入为3个候选网络各自5维指标共150个特征。实验采用50组随机种子（200$\sim$249），模拟无人机在不同初始位置和高度条件下执行遥感巡航任务。仿真时长为10~s，决策周期为0.1~s，每组实验产生100次决策。为模拟实际飞行中的位置偏差和高度波动，启用位置扰动（30~m）和高度扰动（10~m）。

实验除LAAVHA和ALERA外，还运行了TOPSIS-Q、Fuzzy-VHO、SAW、VIKOR、GRA、COPRAS、SPOTIS和最强信号法等8种对比算法以及LAAVHA-L、LAAVHA-A两种消融变体。

\subsection{对比算法横向比较}

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{fig_handover_count_by_algorithm.png}
\caption{11种算法平均切换次数对比（mean $\pm$ std, n=50）}
\label{fig:ho_comparison}
\end{figure}

如图\ref{fig:ho_comparison}所示，在50组随机场景的统计中，LAAVHA以0.24次平均切换在所有算法中表现最优。对比算法中，最强信号法平均切换1.58次（std=0.49），SAW为2.00次（std=0.00），TOPSIS-Q为4.04次（std=1.17），Fuzzy-VHO为8.64次（std=1.11），VIKOR为13.52次（std=6.27），GRA为19.24次（std=1.32），SPOTIS为23.88次（std=3.24），COPRAS为25.88次（std=1.23）。消融变体LAAVHA-L和LAAVHA-A分别为1.08次和2.00次，两者均高于完整LAAVHA，验证了时序预测和动态权重对决策稳定性的关键贡献。

\subsection{代表性算法决策过程对比}

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{fig_scoring_timeline_comparison.png}
\caption{LAAVHA/TOPSIS-Q/Fuzzy-VHO候选网络评分与SINR时序对比（seed=200）}
\label{fig:scoring}
\end{figure}

图\ref{fig:scoring}以2行$\times$3列布局对比了三种算法在同一随机种子下的决策过程。上排子图的纵轴为贴近度，下排为SINR。红色竖虚线标记切换发生时刻。可以观察到，LAAVHA的5G贴近度在全过程中保持稳定高水平（0.99$\sim$1.00），结合双重滞后机制全程未触发任何切换；TOPSIS-Q在WiFi贴近度接近时发生了数次切换，Fuzzy-VHO因模糊规则对SINR波动敏感而产生多次切换。

\subsection{消融实验验证}

\begin{figure}[htb]
\centering
\includegraphics[width=0.65\textwidth]{fig_laavha_handover_count.png}
\caption{消融实验：LSTM与Attention模块贡献对比（n=50）}
\label{fig:ablation}
\end{figure}

为量化LSTM预测模块和注意力动态权重模块对决策质量的贡献，设计消融实验。LAAVHA-L为移除LSTM预测分支的版本，LAAVHA-A为移除注意力动态权重的版本。如图\ref{fig:ablation}所示，完整LAAVHA平均切换仅0.24次（std=0.43）；LAAVHA-L平均切换1.08次（std=1.01），失去时序预测后算法退化为被动反应式决策；LAAVHA-A平均切换2.00次（std=0.00），全部50次运行均产生2次切换。注意力模块的缺失影响最为显著（平均切换从0.24次增至2.00次，增幅达8倍），说明动态权重调节对区分不同飞行阶段的网络需求具有不可替代的作用。两个模块的消融结果从正反两面共同验证了LAAVHA架构设计的合理性。

\subsection{增强机制验证}

\begin{figure}[htb]
\centering
\includegraphics[width=0.85\textwidth]{fig_adaptive_hysteresis_proof.png}
\caption{自适应滞后机制验证：固定低阈值(0.03)与ALERA在$\pm$20dB SINR振荡场景下的对比}
\label{fig:enhanced}
\end{figure}

为直观展示两项增强机制的优势，采用合成竞争场景进行原理验证。构造网络A（类比5G）与网络B（类比WiFi）贴近度在0.50附近拉锯的竞争场景：Zone 1（$t=5\sim8$~s）为真实退化，Zone 2（$t=12\sim15$~s）为瞬时波动。图\ref{fig:enhanced}的4行子图展示了对比结果。固定低阈值方案共触发4次切换——除Zone 1的1次正确切换外，Zone 2内出现2次误切换加1次乒乓切换。ALERA的自适应滞后与风险罚分协同作用下仅触发2次切换，全部位于Zone 1和Zone 2结束后——Zone 2内无任何误触发。自适应阈值在Zone 2波动期间自动从0.03抬升至约0.09，远高于固定基线0.03，波动消退后平滑恢复至0.03。

\begin{table}[htb]
\centering
\caption{增强前后切换次数对比}
\small
\begin{tabular}{cccc}
\toprule
\textbf{方案} & \textbf{平均切换次数} & \textbf{标准差} & \textbf{最终接入网络} \\
\midrule
LAAVHA & 0.24 & 0.43 & 5G（50/50） \\
ALERA & 0.22 & 0.42 & 5G（50/50） \\
\bottomrule
\end{tabular}
\end{table}

两项机制分别决定“何时切换”与“切向谁”，在不改变模型结构的前提下为算法赋予信道波动感知能力。

%========== 4 结束语 ==========
\section{结束语}
\label{sec:conclusion}

面向无人机遥感监测中广域飞行时的多网络切换需求，本文提出了基于LSTM-Attention的LAAVHA自适应垂直切换方法，并在此基础上引入了自适应滞后与风险敏感TOPSIS两项增强机制。该方法以“时序预测+动态权重+改进TOPSIS+双重滞后”四层架构实现候选网络评估与切换决策，增强机制进一步引入上下文感知的滞后参数和置信下界风险罚分，在不重新训练模型的条件下提升信道波动适应性。基于ns-3.45与ns3-ai的决策级实验（11算法$\times$50种子=550次独立运行）表明：LAAVHA在50组随机场景中平均切换仅0.24次（std=0.43），在所有对比算法中表现最优——TOPSIS-Q为4.04次、Fuzzy-VHO为8.64次、SAW为2.00次、VIKOR为13.52次、GRA为19.24次、COPRAS为25.88次、SPOTIS为23.88次、最强信号法为1.58次。消融实验中LAAVHA-L为1.08次、LAAVHA-A为2.00次，验证了两模块的协同贡献。增强机制验证表明，自适应滞后能够在信道振荡时自动抬升切换阈值以抑制乒乓，风险敏感TOPSIS在评分竞争场景下倾向选择历史表现更稳定的网络。当前实验仍存在以下局限：5G采用P2P代理链路而非真实NR协议栈；切换为决策级记录而非协议层执行。后续将引入真实5G NR模块和协议级切换机制，在多样化信道条件和遥感任务剖面下对LAAVHA及ALERA进行完整的性能验证与参数调优。

%========== 参考文献 ==========
\begin{thebibliography}{30}
\bibitem{ref1} YANG K, WANG Y, GAO X, et al. Communications in space-air-ground integrated networks: an overview[J]. Space: Science \& Technology, 2025, 5: 0199.
\bibitem{ref2} MOZAFFARI M, SAAD W, BENNIS M, et al. A tutorial on UAVs for wireless networks[J]. IEEE Communications Surveys \& Tutorials, 2019, 21(3): 2334-2360.
\bibitem{ref3} RIBEIRO L M B, MÜLLER I, BECKER L B. Communication interface manager for improving performance of heterogeneous UAV networks[J]. Sensors, 2021, 21(13): 4255.
\bibitem{ref4} AYASS T, COQUEIRO T, CARVALHO T, et al. Unmanned aerial vehicle with handover management fuzzy system for 5G networks[J]. Intelligence \& Robotics, 2022, 2(1): 20-36.
\bibitem{ref5} WANG Z, LV Z, XU X, et al. Vertical switching algorithm for UAV in power grid heterogeneous communication networks[J]. Electronics, 2024, 13(13): 2612.
\bibitem{ref7} GOUTAM S, UNNIKRISHNAN S, KARANDIKAR A. Algorithm for handover decision based on TOPSIS[C]//IEEE Comnetsat, 2020: 306-313.
\bibitem{ref10} SATAPATHY P, MAHAPATRO J. An efficient multicriteria-based vertical handover decision-making algorithm[J]. Trans Emerging Telecom Tech, 2022, 33(4): e4409.
\bibitem{ref12} TAN K, BREMNER D, LE KERNEC J, et al. Intelligent handover algorithm for V2N with double-deep Q-learning[J]. IEEE Trans Veh Tech, 2022, 71(7): 7848-7862.
\bibitem{ref15} HOCHREITER S, SCHMIDHUBER J. Long short-term memory[J]. Neural Computation, 1997, 9(8): 1735-1780.
\bibitem{ref16} KAUR G, GOYAL R K, MEHTA R. Efficient handover mechanism for 5G using hybridization of LSTM and SVM[J]. Multimedia Tools Appl, 2022, 81(26): 37057-37085.
\bibitem{ref17} MOLLEL M S, ABUBAKAR A I, OZTURK M, et al. Survey of ML applications to handover management in 5G[J]. IEEE Access, 2021, 9: 45770-45802.
\bibitem{ref18} SOYDANER D. Attention mechanism in neural networks[J]. Neural Computing and Applications, 2022, 34(16): 13371-13385.
\bibitem{ref19} MANZOOR S, et al. Which simulator: ns-3 or OMNeT++[J]. Engineering Proceedings, 2023, 46(1): 36.
\bibitem{ref20} YIN H, LIU P, LIU K, et al. ns3-ai[C]//ACM Workshop on ns-3, 2020: 57-64.
\bibitem{ref22} ZOLFANI S, YAZDANI M, PAMUCAR D, et al. VIKOR and TOPSIS reanalysis[J]. Facta Univ Mech Eng, 2020, 18(3): 341-355.
\bibitem{ref23} 赵雪圻, 崔玉波. 乒乓切换问题的区域用户预测研究[J]. 山东通信技术, 2022, 42(4): 40-42.
\bibitem{ref24} YU H, MA Y, YU J. Network selection algorithm for heterogeneous wireless networks[J]. IEEE Access, 2019, 7: 46240-46260.
\bibitem{ref25} TAN X, CHEN G, SUN H. Vertical handover based on multi-attribute and neural network[J]. EURASIP JWCN, 2020, 2020(1): 202.
\bibitem{ref27} PATRICIELLO N, LAGEN S, BOJOVIC B, et al. E2E simulator for 5G NR[J]. Simulation Modelling Practice and Theory, 2019, 96: 101933.
\bibitem{ref28} RAPPAPORT T S. Wireless Communications: Principles and Practice[M]. 2nd ed. Prentice Hall, 2002.
\bibitem{ref29} VASWANI A, SHAZEER N, PARMAR N, et al. Attention is all you need[C]//NIPS, 2017: 5998-6008.
\bibitem{ref30} HWANG C L, YOON K. Multiple Attribute Decision Making[M]. Springer, 1981.
\end{thebibliography}

\end{document}
